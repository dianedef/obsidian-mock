export class Keymap {
    pushScope(scope) {
        // Implementation
    }
    popScope(scope) {
        // Implementation
    }
    static isModifier(evt, modifier) {
        return false;
    }
    static isModEvent(evt) {
        return false;
    }
}
